[build-system]
requires = ["setuptools>=77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "carter-kkt-tesseract"
version = "0.1.0"
description = "Differentiable Carter/KKT dynamics and a Theory 3.3 NR PDE solver"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
dependencies = [
    "numpy>=2.3,<3",
]

[project.optional-dependencies]
training = [
    "torch>=2.6",
]
shader = [
    "matplotlib==3.10.0",
    "numpy==2.5.1",
    "slangpy==0.43.1",
]

[tool.setuptools]
packages = ["tesseract_nr"]
py-modules = [
    "export_hybrid",
    "export_theory33_frozen_slang",
    "fit_theory33_basin",
    "generate_theory33_kinetic_data",
    "hybrid_tesseract",
    "migrate_full_slang",
    "progressive_full_unroll",
    "run_tesseract_nr",
    "run_tesseract_nr_constrained",
    "run_tesseract_nr_convergence",
    "run_theory33_experiments",
    "run_theory33_advanced",
    "run_theory33_hybrid",
    "theory33_advanced",
    "theory33_experiments",
    "theory33_hybrid",
    "theory33_kinetic_data",
    "train_hybrid",
    "train_lyapunov_conditioned",
    "visualize_tesseract",
]
